# ── Generated artifacts (do not commit) ──────────────────────────────────────
# The ONLY source file under data/ is data/seed.py (the generator); every .json
# and .log under data/ is produced by the pipeline/experiments and regenerates.
data/*.json
data/**/*.json
data/**/*.log
data/compare/
data/instances/

# Documento generado desde INFORME.md (regenerar con make_pdf.py)
INFORME.html


# Stray outputs if ever written at the repo root
*.log
dataset.json
repaired_dataset.json
inconsistencies.json
textual_inconsistencies.json
repair_plan.json

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python

# ── Virtual environments ──────────────────────────────────────────────────────
.venv/
venv/
env/
.env

# ── OS / editor / tooling ─────────────────────────────────────────────────────
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

